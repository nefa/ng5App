import { FiltermultiPipe } from './filtermulti.pipe';

describe('FiltermultiPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltermultiPipe();
    expect(pipe).toBeTruthy();
  });
});
